<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinAzureApp.View.DetailPage">
    <ContentPage.Content>
        <StackLayout>
            <StackLayout>
                <Button
                    Text="ELIMINAR"
                    Command="{Binding comandoEliminar}"/>
                <Label Text="{Binding CiudadSeleccionada.Name}" 
                       HorizontalOptions="CenterAndExpand" />
                <Image Source="{Binding CiudadSeleccionada.Image}"
                       HorizontalOptions="Fill"/>
                <Label Text="{Binding CiudadSeleccionada.Description}"
                       HorizontalOptions="CenterAndExpand" />
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>